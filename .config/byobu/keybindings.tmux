
unbind -n F9
unbind -n F1
unbind -n M-Left
unbind -n M-Right
unbind -n M-Up
unbind -n M-Down
bind-key -n C-S-F9 new-window -k -n config byobu-config
unbind-key -n C-a
set -g prefix ^A
set -g prefix2 ^A
bind a send-prefix
